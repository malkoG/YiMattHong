# frozen_string_literal: true

require "rails_helper"

RSpec.describe DiscordNotifier, type: :service do
  describe "#some_method" do
    context "if something happened" do
      it "should be happened to something" do

      end
    end
  end
end
